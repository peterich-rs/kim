name: kim_lints
description: KIM analyzer rules. Business state belongs in Riverpod, not setState.
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  analyzer: ">=7.0.0 <10.0.0"
  analyzer_plugin: ">=0.12.0 <0.15.0"
  custom_lint_builder: ^0.8.0

dev_dependencies:
  lints: ^6.0.0
