{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kim.local/schemas/logic-ota-manifest.json",
  "title": "Kim Android Logic SO OTA Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "host_line",
    "min_host_version_code",
    "max_host_version_code",
    "engine_build_id",
    "logic_version",
    "abi",
    "channel",
    "artifacts",
    "zip_sha256"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "host_line": {
      "type": "string",
      "minLength": 1,
      "description": "Store APK compatibility line; bump on platform/engine/plugin changes."
    },
    "min_host_version_code": {
      "type": "integer",
      "minimum": 1
    },
    "max_host_version_code": {
      "type": "integer",
      "minimum": 1
    },
    "engine_build_id": {
      "type": "string",
      "minLength": 1,
      "description": "Flutter/engine id the AOT was built against, e.g. 3.47.2."
    },
    "logic_version": {
      "type": "string",
      "minLength": 1
    },
    "abi": {
      "type": "string",
      "const": "arm64-v8a"
    },
    "channel": {
      "type": "string",
      "enum": ["stable", "beta", "dev"]
    },
    "created_at": {
      "type": "string",
      "description": "ISO-8601 UTC timestamp."
    },
    "zip_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "artifacts": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "sha256", "size"],
        "properties": {
          "name": {
            "type": "string",
            "enum": ["libapp.so", "libkim_client_ffi.so"]
          },
          "sha256": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "size": {
            "type": "integer",
            "minimum": 1
          }
        }
      }
    },
    "notes": {
      "type": "string"
    }
  }
}
