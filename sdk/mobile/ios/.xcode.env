# Sourced by ios/Scripts/run_xcode_backend.sh.
# Xcode GUI script phases do not inherit ~/.zshrc PATH; rustup lives in ~/.cargo/bin.
# Override locally with .xcode.env.local (gitignored).
export PATH="${HOME}/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:${PATH}"
