[self]
service_id = "chat-1"
service_name = "chat"
listen = "127.0.0.1:8002"
protocol = "tcp"
# empty = Memory. REDIS_URL in main overrides when non-empty.
redis_url = ""
# empty = Memory message store. DATABASE_URL overrides when non-empty.
database_url = ""
db_max_connections = 5
db_acquire_timeout_ms = 3000
db_idle_timeout_secs = 60
# empty = in-process Memory directory + store. ROYAL_URL overrides.
royal_url = ""
snowflake_node = 1
zone = "zone_local"
metrics_listen = "127.0.0.1:9002"
# empty = filters still run but pass every payload
sensitive_words = []
blocked_image = []
